1	Created MVC WebApp
	a	Select Authentication - Individual User Accounts
2	In appsettings.json - ConnectionStrings 
		a	-update the server and the database:
			"DefaultConnection": "Server=.\\SQLExpress;Database=RhymeBinder;Trusted_Connection=True;MultipleActiveResultSets=true"
			
3	Run and register a new user and, when prompted
	a	click Run Migrations at error message
	b	Refresh page
	
4	Check SSMS to be sure identitiy tables were created

5	In Package Manager Console, ran the following:
	a	Install-Package Microsoft.EntityFrameworkCore
	b 	Install-Package Microsoft.EntityFrameworkCore.SqlServer
	c   Install-Package Microsoft.EntityFrameworkCore.Tools
	d	Scaffold-DbContext "Server=.\SQLExpress;Database=RhymeBinder;Trusted_Connection=True;" Microsoft.EntityFrameworkCore.SqlServer -OutputDir Models
	
6	Added the following to Startup.cs : ConfigureServices(IService Connection services)
	a	var connection = "Server=.\\SQLExpress;Database=RhymeBinder;Trusted_Connection=True;ConnectRetryCount=0;";
	b   services.AddDbContext<DatabaseNameContext>(options => options.UseSqlServer(connection));
	
7	Created RhymeBinderController
	a	added the following:
	
        private readonly RhymeBinderContext _context;

        public RhymeBinderController(RhymeBinderContext context)
        {
            _context = context;
        }
		
8 	did not work after this, so in Startup.CS I commneted app.UseDatabaseErrorPage
 public void Configure(IApplicationBuilder app, IWebHostEnvironment env)
        {
            if (env.IsDevelopment())
            {
                app.UseDeveloperExceptionPage();
                //app.UseDatabaseErrorPage();
            }
			
	
--Update - I just moved the "production" error returning into the if (env.IsDevelopment())

9 add [Authorize] to controller so, y'know, you need to log in		
			
			
Scaffold-DbContext "Server=.\SQLExpress;Database=RhymeBinder;Trusted_Connection=True;"  Microsoft.EntityFrameworkCore.SqlServer -OutputDir Models -force			